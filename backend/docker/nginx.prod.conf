server {
    listen 80;
    server_name api.grosirun.id;
    root /var/www/html/public;
    index index.php;

    client_max_body_size 10M;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip
    gzip on;
    gzip_types text/plain application/json application/javascript text/css image/svg+xml;
    gzip_min_length 1024;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 60s;
    }

    location ~ /\. {
        deny all;
    }

    location /storage/ {
        # For local fallback if S3 not used, but S3 primary prod no need
        try_files $uri $uri/ =404;
    }
}

# SSL handled by host Nginx or Certbot, this is inside Docker nginx for prod?
# For blue-green, host Nginx 443 terminates SSL and proxies to Docker nginx 80
