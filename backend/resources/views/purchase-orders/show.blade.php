@extends('layouts.app')@section('title','Detail PO')@section('content')<x-ui-page-header title="PO #{{ $po->uuid }}" /><div class="grid lg:grid-cols-3 gap-6"><div class="lg:col-span-2 space-y-6"><x-ui-card><h3 class="text-title-lg mb-4">Detail Purchase Order</h3><div class="grid sm:grid-cols-2 gap-4"><div><p class="text-body-sm text-text-secondary">Inisiator</p><p class="text-body-md font-medium">{{ $po->initiator->name ?? '-' }}</p></div><div><p class="text-body-sm text-text-secondary">Supplier</p><p class="text-body-md font-medium">{{ $po->supplier->name ?? '-' }}</p></div><div><p class="text-body-sm text-text-secondary">Campaign</p><p class="text-body-md font-medium">{{ $po->campaign->title ?? '-' }}</p></div><div><p class="text-body-sm text-text-secondary">Quantity</p><p class="text-body-md font-medium">{{ $po->total_quantity }}</p></div><div><p class="text-body-sm text-text-secondary">Harga / unit</p><p class="text-body-md font-medium">Rp{{ number_format($po->unit_price,0,',','.') }}</p></div><div><p class="text-body-sm text-text-secondary">Total</p><p class="text-title-lg text-primary-500">Rp{{ number_format($po->total_amount,0,',','.') }}</p></div></div></x-ui-card><x-ui-card><h3 class="text-title-lg mb-4">Upload Dokumen</h3><div x-data="filepondUploader({processUrl:'{{ route('purchase-orders.upload-document',$po->uuid) }}',multiple:true,label:'Seret invoice/surat jalan ke sini atau <span class=\\'filepond--label-action\\'>pilih file</span>'})"><input type="file" x-ref="pond" name="documents[]" multiple></div></x-ui-card></div><div class="space-y-6"><x-ui-card><div class="flex items-center gap-3 mb-4"><i data-lucide="info" class="w-5 h-5 text-info-500"></i><span class="text-title-md">Status</span></div><x-ui-status-badge :status="$po->status" /><div class="mt-4 space-y-2">@if($po->status==='submitted')<form method="POST" action="{{ route('purchase-orders.accept',$po->uuid) }}">@csrf<x-ui-button type="submit" variant="primary" icon="check" class="w-full">Accept PO</x-ui-button></form><form method="POST" action="{{ route('purchase-orders.reject',$po->uuid) }}" x-data="confirmAction()">@csrf<input type="hidden" name="reason" value="Tidak sesuai"><x-ui-button type="submit" variant="danger" icon="x" class="w-full" @click.prevent="confirm('Yakin tolak PO?').then(ok=>ok&&$el.closest('form').submit())">Reject PO</x-ui-button></form>@elseif($po->status==='accepted')<form method="POST" action="{{ route('purchase-orders.confirm-payment',$po->uuid) }}">@csrf<x-ui-button type="submit" variant="primary" icon="banknote" class="w-full">Konfirmasi Pembayaran</x-ui-button></form>@elseif($po->status==='paid')<form method="POST" action="{{ route('purchase-orders.update-status',$po->uuid) }}">@csrf<input type="hidden" name="status" value="processing"><x-ui-button type="submit" variant="primary" icon="work" class="w-full">Proses</x-ui-button></form>@elseif($po->status==='processing')<form method="POST" action="{{ route('purchase-orders.update-status',$po->uuid) }}">@csrf<input type="hidden" name="status" value="shipped"><x-ui-button type="submit" variant="primary" icon="truck" class="w-full">Tandai Dikirim</x-ui-button></form>@endif</div></x-ui-card></div></div>@endsection
